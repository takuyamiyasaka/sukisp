<h1>回答</h1>
	<%= @inquiry.request %>
<div class="inq-edit">
	<%= render "admins/inquiries/inq_edit",inquiry: @inquiry %>
</div>
<%= form_for(@answer,url:admins_answers_path) do |f| %>
	<%= f.text_area :answer %>
	<%= f.hidden_field :inquiry_id %>
	<%= f.submit "送信" %>
<% end %>

